apiVersion: v1
kind: Service
metadata:
  name: {{ include "deerflow.fullname" . }}-frontend
  labels:
    {{- include "deerflow.labels" . | nindent 4 }}
    app.kubernetes.io/component: frontend
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "deerflow.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: frontend
