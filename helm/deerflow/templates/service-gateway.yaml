apiVersion: v1
kind: Service
metadata:
  name: {{ include "deerflow.fullname" . }}-gateway
  labels:
    {{- include "deerflow.labels" . | nindent 4 }}
    app.kubernetes.io/component: gateway
spec:
  type: ClusterIP
  ports:
    - port: 8001
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "deerflow.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: gateway
