# InS 大模型技术架构说明

> **文档编号**：2026-001
> **版本**：v2.0
> **生成日期**：2026-06-05

---

## 一、大模型类型

**类型：多模态大语言模型**

### 1.1 模型规格

| 参数项 | 规格 |
|--------|------|
| 基础模型 | qwen3.6-27b、qwen3.6-35b-a3b（MoE架构） |
| 输入模态 | 文本、图谱 |
| 输出模态 | 文本 |
| 上下文长度 | 256K tokens |
| 最大输入长度 | 254K tokens |
| 最大输入长度（思考模式） | 252K tokens |
| 最大输出长度 | 64K tokens |
| 最大思维链长度 | 128K tokens |

### 1.2 核心能力

| 能力维度 | 支持情况 | 应用场景 |
|----------|----------|----------|
| 自然语言理解与生成 | 支持 | 工业领域专业文本生成、知识问答、报告撰写 |
| 图谱理解 | 支持 | 工业图谱分析、设备状态识别、异常模式检测 |
| 深度推理（思维链） | 支持 | 复杂故障诊断、根因分析、多步推理 |

### 1.3 技术特性

- **多模型协同**：采用多模型输出交叉对比验证优选机制，提升输出准确性和可靠性
- **内容安全**：内置敏感词识别及过滤机制，保障输出内容合规
- **系统提示词控制**：通过系统提示词约束模型行为边界，确保输出符合工业领域规范

---

## 二、应用系统架构

**架构类型：工具增强型大模型应用系统**

### 2.1 系统组成

| 层级 | 说明 |
|------|------|
| 大模型层 | qwen3.6系列多模态模型，提供语言理解、图谱识别、推理能力 |
| 知识库层 | 自有工业领域知识库，通过系统提示词控制调用 |
| 工具层 | 通过 tools 机制调取 InS 系统数据，实现数据接入 |
| 应用层 | 异常数据分析、异常预警、故障诊断、诊断报告生成 |

### 2.2 核心功能

| 功能模块 | 说明 |
|----------|------|
| 数据接入 | 通过 tools 调取 InS 系统实时运行数据 |
| 异常检测 | 分析设备运行数据，识别异常模式和趋势 |
| 故障预警 | 基于异常数据进行预警判断，提前发现潜在风险 |
| 故障诊断 | 结合知识库进行根因分析，生成诊断报告 |
| 质量保障 | 多模型输出交叉对比验证，优选最佳结果 |

### 2.3 系统特性

- **工具调用能力**：通过 tools 机制与 InS 系统深度集成，实现数据实时接入
- **知识库增强**：自有工业领域知识库，提供专业领域知识支撑
- **持久化记忆**：基于 LLM 的对话记忆提取与事实库存储
- **技能扩展**：可插拔技能系统，支持行业专用能力（日报/周报/月报/故障诊断等）
- **上下文管理**：自动摘要压缩、上下文窗口管理，支持超长对话

---

## 三、数据安全与隔离机制

| 隔离维度 | 实现方式 |
|----------|----------|
| 租户隔离 | 数据库行级租户标识、向量库集合命名隔离、知识库权限矩阵 |
| 用户隔离 | 文件系统路径隔离（`users/{user_id}/`）、记忆存储隔离、技能配置隔离 |
| 线程隔离 | 对话线程独立目录（`threads/{thread_id}/`）、工件存储隔离、上传文件隔离 |

**数据流安全**：

- 敏感信息（密码、令牌、密钥）禁止在 UI 区块渲染
- 文件上传自动检测加密 PDF 并拒绝处理
- 所有路径操作经过 `resolve()` + `relative_to()` 容器校验

---

## 四、审计与监控

| 审计类型 | 说明 |
|----------|------|
| 沙箱命令审计 | 所有 bash 命令执行记录审计日志，危险命令模式拦截（`rm -rf /`、管道注入等） |
| 内容安全审计 | 管理端可查询内容安全审计日志，敏感词识别及过滤记录 |
| 追踪系统 | 支持 LangSmith 等追踪提供商集成，记录系统执行链路 |
| Token 用量统计 | 可配置启用 Token 用量追踪中间件 |

---

## 五、权限控制

**认证机制**：

- JWT Token 认证（HS256 算法，可配置过期时间）
- RSA 公钥验证
- API Key 认证

**RBAC 角色模型**：

| 角色 | 权限范围 |
|------|----------|
| superadmin | 系统级管理、租户管理、全局配置 |
| tenant_admin | 租户级管理、技能管理、知识库管理 |
| user | 个人对话、自定义技能、私有知识库 |

**权限控制点**：

- 知识库访问需验证租户所有权
- 技能 CRUD 需租户管理员角色
- 报告发布/验证需特定权限（`closure:verify`）

---

## 六、合规相关技术实现

| 合规要求 | 技术实现 |
|----------|----------|
| 数据不出境 | 支持私有化部署，模型服务可接入境内 API 网关 |
| 操作可追溯 | 全链路审计日志、线程级工件存储 |
| 最小权限原则 | RBAC 角色分级、工具组按需授权 |
| 输入校验 | 技能名称正则校验、ID 格式校验、路径容器校验 |
| 错误处理 | 标准化错误码、用户友好提示、服务端详细日志 |
| 内容安全 | 敏感词识别及过滤机制，保障输出内容合规 |

---

## 附录：备案问题回答模板

**问题：贵司大模型的类型是什么，单模态还是多模态或者是智能体？**

**回答**：

我司大模型为**多模态大语言模型**，基于 qwen3.6-27b 和 qwen3.6-35b-a3b（MoE架构）构建。支持文本和图谱输入、文本输出，上下文长度256K tokens，最大思维链长度128K tokens。具备自然语言理解、图谱识别、深度推理能力，采用多模型交叉验证机制提升输出准确性，内置敏感词识别及过滤机制保障内容安全。

基于该大模型，我司构建了**工具增强型应用系统**，通过 tools 机制接入 InS 系统数据，结合自有知识库，实现设备异常检测、故障预警、故障诊断及诊断报告生成。

---
