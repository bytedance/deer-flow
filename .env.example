# 搜索工具
# FIRECRAWL_API_KEY=your-firecrawl-api-key

# Serper API 密钥（Google 搜索）- https://serper.dev
SERPER_API_KEY=your-serper-api-key

# TAVILY API 密钥
TAVILY_API_KEY=your-tavily-api-key

# Jina API 密钥
JINA_API_KEY=your-jina-api-key

# InfoQuest API 密钥
INFOQUEST_API_KEY=your-infoquest-api-key

# 浏览器 CORS 白名单（逗号分隔的精确来源）。
# nginx 移除后必需：浏览器现在跨域直连 Gateway 的 :8001。
# serve.sh 与 docker-compose 文件已自动设置默认值；
# 仅在前端运行于非默认主机/端口时才需要覆盖。
# GATEWAY_CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# 模型API KEY
# 360智脑
# QIHOO_360_API_KEY=your-qihoo-api-key # AopenAI 兼容，参见 https://ai.360.com/docs
# 火山引擎
# VOLCENGINE_API_KEY=your-volcengine-api-key
# OPENAI_API_KEY=your-openai-api-key
# GEMINI_API_KEY=your-gemini-api-key
# DEEPSEEK_API_KEY=your-deepseek-api-key
# NOVITA_API_KEY=your-novita-api-key  # OpenAI 兼容，参见 https://novita.ai
# MINIMAX_API_KEY=your-minimax-api-key  # OpenAI 兼容，参见 https://platform.minimax.io
# STEPFUN_API_KEY=your-stepfun-api-key  # OpenAI 兼容，参见 https://platform.stepfun.com
# VLLM_API_KEY=your-vllm-api-key  # OpenAI 兼容

# IM 渠道
# FEISHU_APP_ID=your-feishu-app-id
# FEISHU_APP_SECRET=your-feishu-app-secret
# SLACK_BOT_TOKEN=your-slack-bot-token
# SLACK_APP_TOKEN=your-slack-app-token
# TELEGRAM_BOT_TOKEN=your-telegram-bot-token
# DISCORD_BOT_TOKEN=your-discord-bot-token
# WECOM_BOT_ID=your-wecom-bot-id
# WECOM_BOT_SECRET=your-wecom-bot-secret
# DINGTALK_CLIENT_ID=your-dingtalk-client-id
# DINGTALK_CLIENT_SECRET=your-dingtalk-client-secret

# 启用 LangSmith 以监控并调试你的 LLM 调用、Agent 运行和工具执行。
# LANGSMITH_TRACING=true
# LANGSMITH_ENDPOINT=https://api.smith.langchain.com
# LANGSMITH_API_KEY=your-langsmith-api-key
# LANGSMITH_PROJECT=your-langsmith-project

# GitHub API 令牌
# GITHUB_TOKEN=your-github-token

# 数据库（仅当 config.yaml 中 database.backend 为 postgres 时需要）
# DATABASE_URL=postgresql://deerflow:password@localhost:5432/deerflow

# 在生产环境中将其设置为 "false" 可禁用 Swagger UI、ReDoc 和 OpenAPI schema
# GATEWAY_ENABLE_DOCS=false

# 多 worker 部署所共享的内部 Gateway 认证令牌。
# `make up` 会自动生成并持久化该令牌；仅当你在自带部署脚本之外
# 运行 Gateway worker 时才需手动设置。
# DEER_FLOW_INTERNAL_AUTH_TOKEN=your-shared-internal-token

# ── 前端 SSR → Gateway 配线 ─────────────────────────────────────────────
# Next.js 服务器使用以下变量在 SSR 期间访问 Gateway（认证检查、
# /api/* 重写）。它们默认匹配 `make dev` 与 `make start` 的 localhost 值，
# 因此大多数本地用户无需设置。
#
# 仅当 Gateway 不在 localhost:8001（例如前端与 gateway 运行在不同主机、
# 在带服务别名的容器中或在不同端口背后）时才覆盖。docker-compose 已自动设置这些值。
# DEER_FLOW_INTERNAL_GATEWAY_BASE_URL=http://localhost:8001
# DEER_FLOW_TRUSTED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# ── Claude Code / Codex CLI 订阅作为模型提供方（可选） ───────
# 如果你配置了复用 CLI 订阅登录的 ClaudeChatModel / Codex 模型提供方
#（或 ACP agent），建议通过环境变量传入 token，而不是将整个
# ~/.claude / ~/.codex 绑定挂载到容器中。Gateway 凭证加载器会优先读取
# 这些变量，因此无需挂载目录。
#   CLAUDE_CODE_CREDENTIALS_PATH 指向单个 .credentials.json (Claude)
#   而不是整个目录。docker-compose.cli-auth.yaml 是
#   面向需要完整 CLI 配置的适配器的可选目录挂载回退方案。
#   ACP 适配器通常使用自己的环境变量 API key（例如 ANTHROPIC_API_KEY），
#   完全无需挂载——请参阅适配器文档。详见 SECURITY.md。
# CLAUDE_CODE_OAUTH_TOKEN=your-claude-code-oauth-token
# ANTHROPIC_AUTH_TOKEN=your-anthropic-auth-token
# CLAUDE_CODE_CREDENTIALS_PATH=/path/to/.claude/.credentials.json
# CODEX_AUTH_PATH=/path/to/codex/auth.json
