"""Programmatic Tool Calling (PTC) — sandbox-to-MCP bridge.

This package implements client-side programmatic tool calling, where
sandbox Python code can invoke MCP tools as regular Python function calls
through a host-side HTTP proxy.  API keys stay on the host and never
enter the sandbox.
"""
