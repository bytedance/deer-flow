# Agents

This directory will contain the Super-Agent orchestration implementation (deerflow-inspired).

Planned modules:
- orchestrator.py (graph runner)
- agents/ (lead agent, subagents: writer, visual_director, animator)
- adapters/ (llm_adapter.py, vision_adapter.py, tts_adapter.py)

