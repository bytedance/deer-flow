# 组织化数字分身平台选型与 PoC 方案

**日期：** 2026-07-15

**状态：** 已确定组合路线（DeerFlow + 自建治理层）

---

## 一、 背景与目标

### 1.1 背景

王国栋已在 ChatGPT 中实践个人数字分身（含“过去/现在/未来”、元中枢、M 系列智能体等），形成了可感知的个人能力样板。当前核心问题是**如何将个人能力转化为组织可控、可复制、可扩展的数字员工能力**。现阶段不确定平台搭建细节，第一阶段应先进行平台选型标准制定和最小 PoC，而非直接建设完整平台。

### 1.2 目标（回答四个核心问题）

1. 组织化数字分身平台最低需具备哪些能力？


2. 个人分身资产如何整理成可迁移、可测试的组织样本？


3. 候选平台能否承载数字员工的核心能力？


4. 公司下一阶段应选择哪条平台实施路线？



### 1.3 非目标

* 不自建大模型，不做全员推广，不一次性实现 22 个 M 系列智能体。


* 不预设必须使用 dify。


* 不做纯技术评估，必须使用真实迁移包进行测试。



---

## 二、 核心原则

1. **组织可控优先**：平台必须具备最小治理闭环，包括知识库可控（分层、归属、可追溯）、权限可控（账号权限隔离）、输出可控（防编造/越权）及过程可控（日志留痕）。


2. **用迁移包测试平台**：统一使用王国栋分身迁移包测试候选平台，比较能力保真度与治理能力。


3. **先标准，后平台**：先定义能力标准，再评估 dify、开源平台、低代码平台或自研原型。



---

## 三、 选型结论与组合路线架构

经过对 DeerFlow 的评估，确认采用 **“DeerFlow + 自建治理层”** 的组合路线：

* **Agent 运行时底座（DeerFlow - 字节跳动开源，MIT 许可）**：
* 覆盖**智能体能力**和**集成扩展能力**。


* 提供 LangGraph 底座、Lead Agent 编排、Skill 模块化、Sub-agent 委派、MCP 集成和多模型支持。




* **自建治理层**：
* **知识库能力**：自建 RAG 知识库服务，通过 MCP 接入 DeerFlow。


* **权限治理能力**：自建 Auth/RBAC 中间件，注入 Lead Agent 中间件链。


* **日志追溯能力**：自建审计日志中间件，记录 Agent 周期的输入/输出及来源。


* **评估迭代能力**：自建评估服务，管理测试样本集、评分与对比。




* **模板复制能力**：天然由 DeerFlow 的 Skill + Custom Agent 体系覆盖。



### 3.1 平台对比

# 3.1 平台对比
| 维度 | DeerFlow | dify（待评估） |
| ---- | -------- | -------------- |
| **智能体运行时** | LangGraph 底座，Lead Agent 编排，成熟度高 | 待查 |
| **模型灵活性** | 多模型提供者，YAML 配置热加载，不绑定供应商 | 待查 |
| **开源许可** | MIT，无供应商锁定 | 待查 |
| **扩展性** | MCP 集成、中间件链、自定义工具和技能 | 待查 |
| **组织治理** | 需要自建补齐 | 待查 |
| **运维成熟度** | Docker 部署、Checkpoint 持久化、线程管理 | 待查 |
| **社区活跃度** | 字节跳动维护，GitHub 开源 | 待查 |

### 3.2 部署与技术架构

```
docker-compose.yml
┌──────────────────────────────────────────────┐
│  deerflow-backend    :8001                    │
│    ┌──────────────────────────────────────┐  │
│    │  Lead Agent                          │  │
│    │  ├── AuthMiddleware (注入)            │  │
│    │  └── AuditMiddleware (注入)           │  │
│    └──────────────────────────────────────┘  │
│  deerflow-frontend   :3000                    │
│  kb-mcp              :8101  (ChromaDB 内嵌)   │
│  eval-api            :8104  (Flask/FastAPI)   │
└──────────────────────────────────────────────┘

```

* **自建组件与接口边界**：
* **RAG 知识库** (`kb-mcp` :8101)：通过 MCP Server 接口，在 Agent 需检索知识时调用。


* **Auth/RBAC 校验** (DeerFlow 进程内挂载)：作为 Middleware，在每次 Agent 调用前执行。


* **Audit Log 审计** (DeerFlow 进程内挂载)：作为 Middleware，在 Agent 执行前后记录。


* **Eval API** (`eval-api` :8104)：独立 HTTP API，用于 PoC 测试回合后的质量评估。




* **嵌入模型选型**：首选**火山方舟（字节跳动）** 的 `Doubao embedding`，通过 DeerFlow 原生支持的 `ark` provider 接入，实现 Chat 与 Embedding 凭证统一。



---

## 四、 王国栋分身迁移包与测试设计

### 4.1 迁移包构成

1. **提示词体系**：过去/现在/未来、元中枢、M 系列智能体、问题拆解法。


2. **对话与案例**：真实问题输入、分身输出、人工修正记录。


3. **个人资料**：简历、岗位职责、测评报告、价值观等。


4. **方法论与访谈**：洞察本质/拆解结构等材料、分身构建经验与边界说明。


5. **红线规则**：数据安全、决策权限、事实真实性、人格边界。



### 4.2 案例样本（10 个真实案例）

* **成功案例（5 个）** + **进行中案例（3 个）** + **失败/复盘案例（2 个）**。


* 采用“先粗后细”整理：全量整理基本路径；其中 2 个标杆案例深度整理输入、分析过程、标准输出及防越权边界。



---

## 五、 PoC 核心能力要求与方案设计

### 5.1 平台 7 大能力清单与 PoC 准入标准

1. **智能体能力（第一优先级）**：稳定角色设定，提示词模块化，支持工作流与多智能体（Lead-Sub）调度。


2. **知识库能力**：支持资料分层（公司级/岗位级/个人级），支持来源追溯（PoC 阶段要求检索结果附带行号与文件名）。


3. **权限治理能力**：支持账号角色权限及数据隔离，普通员工默认不暴露王国栋原始资料。


4. **日志追溯能力**：记录调用人、调用的知识/提示词、生成输出、以及采纳状态。


5. **评估迭代能力**：支持固定测试样本集、人工评分以及与 ChatGPT/真人回答对比。


6. **模板复制能力**：能够将王国栋个人分身解耦，生成用于初始化他人分身的方法模板。


7. **集成扩展能力**：支持外部模型调用、未来模型替换及 API 插件扩展（如 MCP）。



### 5.2 账号与边界设计

* **管理员**：负责资料入库、权限配置、模板管理、日志审查。


* **王国栋本人**：使用和校准分身。


* **普通员工**：**数据层隔离**。默认仅可调用方法模板，不可检索王国栋原始个人资料。深度调用需发起授权申请。



---

## 六、 实施节奏与决策输出

### 6.1 实施步骤

暂不考虑



### 6.2 决策输出指标

1. 是否确认“DeerFlow + 自建治理层”为第一阶段承载路线？


2. 明确哪些能力由平台承载，哪些由提示词和业务流程补齐？


3. 评估王国栋分身是否具备向组织推广的迁移价值？



---

## 七、 风险、应对与准入条件

### 7.1 风险及应对预案

* **风险 1：平台能力强但治理弱** $\rightarrow$ **应对**：不进入正式建设，仅作能力原型。


* **风险 2：治理强但模型能力弱于 ChatGPT** $\rightarrow$ **应对**：优化模型接入和知识召回，暂缓全员推广。


* **风险 3：自建治理层范围膨胀** $\rightarrow$ **应对**：严格控制 MVP 范围，PoC 阶段可采用硬编码，不提供 UI 及自动化审批流。


* **风险 4：RAG/模型等依赖服务异常** $\rightarrow$ **应对**：
* **RAG 超时/不可用**：触发 10 秒超时（可重试1次）降级，Agent 输出“知识库暂时不可用，以下回答基于内置记忆”。


* **Auth 权限校验不可用**：采用 **Fail-closed（安全失败）** 策略，拒绝所有请求。


* **日志写入异常**：捕获异常并输出 stderr 告警，不阻断 Agent 主流程。





### 7.2 样板推广阶段准入条件（PoC 阶段不做，作为后续门槛）

1. **第二人分身验证**：必须用第二个人的方法迁移包跑通流程，验证模板复制的可行性。


2. **接口规范文档**：产出 `docs/interface-spec.md`，规范定义自建服务的 MCP tool schema、Middleware 签名及 HTTP API 契约。


3. **部署编排完善**：使用 `docker-compose.yml` 统一编排，且 DeerFlow 对 MCP 的连接需具备重试机制（每 5s 一次，最多 30s）。


4. **提示词防御与冲突指引**：在 Skill 提示词末尾加入硬编码防注入规则；对于评分中“像他”与“有用”出现的矛盾，采取独立打分并标注为评审讨论焦点。