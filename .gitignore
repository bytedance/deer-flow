# DeerFlow docker image cache
docker/.cache/
# oh-my-claudecode state
.omc/
# OS generated files
.DS_Store
*.local
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Python cache
__pycache__/
*.pyc
*.pyo

# Virtual environments
.venv
venv/

# Environment variables
.env
.env.bak

# Configuration files
config.yaml
mcp_config.json
extensions_config.json

# IDE
.idea/
.vscode/

# Coverage report
coverage.xml
coverage/
.deer-flow/
.claude/
.planning/
skills/custom/*
logs/
log/
debug.log

# Local git hooks (keep only on this machine, do not push)
.githooks/

# chatbi-report test runtime-generated driver scripts
backend/tests/chatbi_report/fixtures/_render_driver*.py

# pnpm
.pnpm-store
sandbox_image_cache.tar

# ignore the legacy `web` folder
web/

# Deployment artifacts
backend/Dockerfile.langgraph
config.yaml.bak
.playwright-mcp
/frontend/test-results/
/frontend/playwright-report/
.gstack/
.worktrees
# CodeGraph index
.codegraph/
# Cursor IDE
.cursor/
# npm packages
node_modules/
package-lock.json
# Skills zip
skills.zip
# Root package.json (EARENDIL project, not DeerFlow)
package.json
# Local packaging & cookie artifacts (do not commit)
backend.zip
deer-flow.zip
cookies.txt

# Offline Claude Code bundles (build artifacts; regenerate via scripts/bundle-claude-*.sh on a Linux x86_64 host)
offline-claude/*.tar.gz
offline-claude/*.tgz
offline-claude/*.zip

# Docker pi-agent runtime state (sessions / installed skills; do not commit)
docker/pi-agent/sessions/
docker/pi-agent/skills/
docker/pi-agent/auth.json

# Claude Code local state (startup count, customApiKeyResponses, tips history)
docker/claude/.claude.json
